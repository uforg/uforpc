/**
 * version is replaced during the release process by the latest Git tag
 * and should not be manually edited.
 */
export const version = "0.0.0-dev";
export const versionWithPrefix = `v${version}`;
